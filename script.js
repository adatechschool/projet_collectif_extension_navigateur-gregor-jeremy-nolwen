// Récupération du bouton 25 minutes
let button = document.getElementById("min25");

// Ajout d'un écouteur d'événement "click" sur le bouton
button.addEventListener("click", function () {
  // Modification de la couleur de fond de toutes les pages web en bleu
  document.body.style.backgroundColor = "blue";
});
